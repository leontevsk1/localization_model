1. Статистическое взвешивание невязок в GD (закрывает 02_bad-sensors).
Сейчас дальномерный член имеет вес 1, регуляризация — общий λ. Заменить на веса, обратные дисперсиям источников:                                                                - дальности: вес 1/dist_err²;

- привязка: λᵢ = 1/Uncertaintyᵢ² — индивидуально на узел.
Эффект: при dist_err >> gps_err коррекция автоматически затухает (ratio не упадёт ниже 1), при дрейфе λᵢ сам ослабевает со временем. Один шаг делает систему байесовски согласовубирает ручной подбор λ под сценарий.
                                                                                                                                                                                2. Прокрустова фиксация gauge (закрывает плато 01_bad-gps).
После GD+EKF на каждом тике выравнивать решение по Кабшу к предсказанным координатам (CurrentCoord после счисления, до оптимизации). Оптимизатор перестаёт вносить произвольный сдвиг/поворот каркаса; СК наследуется от GNSS-инициализации. Ошибка формы уходиая абсолютная ошибка — только дрейф счисления.

1. Численная защита GD (закрывает NaN в 04_edge-macro).

- Backtracking: если после шага суммарная невязка выросла — откатить и уполовинить α. Убирает ручной подбор α под масштаб поля (0.0005 для комнаты, 5.0 для 50 км).
- Guard: !math.IsInf/IsNaN(shiftMag), иначе прервать с ошибкой вместо тихой «сх

1. Персистентная ковариация EKF.
RunEKF каждый тик сбрасывает P = I — фильтр забывает накопленную уверенность и заново «доверяет» априори одинаково всем узлам. Хранить P между тиками, в predict добавлять Q — тогда EKF     станет настоящим рекурсивным фильтром, а не пакетным решателем.
                                                                                                                                                                                             5. BelievedCoord как измерение в EKF.
Добавить в вектор измерений псевдо-GPS: zᵢ = BelievedCoordᵢ с R = Uncertaintyᵢ². Фьюжн дальностей и привязки происходит в одном месте с правильными весами; GD остаётся только для cold      start.
                                                                                                                                                                                             6. Топология: симметризация измерений.
k-NN сейчас асимметричный: измерение i→j двигает только i. Учитывать каждую дальность для обоих концов (как в EKF уже сделано через Якобиан, но не в GD) — бесплатное удвоение информации на связь. Плюс 05_dense показал: ratio растёт с плотностью — k=7 при малых сетях с

2. Метрика для контроля.
Разделить Final RMSE на ошибку формы (после прокрустова выравнивания к RealCoord) и ошибку привязки (сдвиг+поворот). Без этого шаги 1–2 не измерить: сейчас обе ошибки смешаны в одном числе.

Порядок внедрения: 1 → 3 (дёшево, чинят два сломанных сценария) → 2 → 7 (измери
